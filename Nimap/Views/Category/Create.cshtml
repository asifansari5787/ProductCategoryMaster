@model Nimap.Models.Category

<div class="container mt-4">
    <h2>Create Category</h2>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal border p-4 rounded shadow-sm">
            <hr />
            <div class="form-group row">
                @Html.LabelFor(model => model.CategoryName, new { @class = "control-label col-md-2 font-weight-bold" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.CategoryName, new { htmlAttributes = new { @class = "form-control", @placeholder = "Enter Category Name" } })
                    @Html.ValidationMessageFor(model => model.CategoryName, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group row mt-3">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-primary" />
                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary ml-2", @style = "margin-left: 10px;" })
                </div>
            </div>
        </div>
    }
</div>
