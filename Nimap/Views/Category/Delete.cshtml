@model Nimap.Models.Category

<div class="container mt-4">
    <h3 class="text-danger">Are you sure you want to delete this category?</h3>
    <div class="border p-4 rounded shadow-sm bg-light">
        <dl class="row">
            <dt class="col-sm-3 font-weight-bold">Category Name</dt>
            <dd class="col-sm-9">@Model.CategoryName</dd>
        </dl>

        <hr />

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <button type="submit" class="btn btn-danger">Delete</button>
                @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary ml-2", @style = "margin-left: 10px;" })
            </div>
        }
    </div>
</div>

